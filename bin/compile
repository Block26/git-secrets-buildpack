#!/bin/bash

indent() {
  sed -u 's/^/       /'
}

echo "-----> Install git-secrets"

mkdir /git-secrets
git clone https://github.com/awslabs/git-secrets.git /git-secrets 2>&1 | indent
ln -s /git-secrets/git-secrets /usr/local/bin/git-secrets
git-secrets --register-aws --global
